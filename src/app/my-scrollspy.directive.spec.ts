import { MyScrollspyDirective } from './my-scrollspy.directive';

describe('MyScrollspyDirective', () => {
  it('should create an instance', () => {
    const directive = new MyScrollspyDirective();
    expect(directive).toBeTruthy();
  });
});
