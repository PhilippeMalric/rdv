import { FilterNcmPipe } from './filter-ncm.pipe';

describe('FilterNcmPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNcmPipe();
    expect(pipe).toBeTruthy();
  });
});
